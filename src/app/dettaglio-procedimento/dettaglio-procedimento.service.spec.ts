/*import { TestBed, inject } from '@angular/core/testing';

import { DettaglioProvvedimentoService } from './dettaglio-provvedimento.service';

describe('DettaglioProvvedimentoService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DettaglioProvvedimentoService]
    });
  });

  it('should be created', inject([DettaglioProvvedimentoService], (service: DettaglioProvvedimentoService) => {
    expect(service).toBeTruthy();
  }));
});
*/